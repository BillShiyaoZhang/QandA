# 首版验证与上线验收

验证日期：2026-09-06，Apple Silicon macOS，Node.js 22.22.3。数据为开发样例，结果是功能与容量检查，不是用户研究或模型能力评测。

## 本地结果

| 项目 | 结果 | 复现方式 |
| --- | --- | --- |
| 内容完整性 | 27 个问题、41 份答案、3 条关联、1 条注释通过 | `npm run validate` |
| 类型检查 | 0 错误、0 警告；1 条 Zod 旧方法提示 | `npm run check` |
| 核心测试 | 40 项通过 | `npm test` |
| 浏览器验收 | 18 项通过，桌面及手机各 9 项，使用 Chromium | `/QandA` 构建后，`TEST_BASE_PATH=/QandA npm run test:e2e -- site.spec.ts` |
| 撤回导出 | 整站构建、节点 JSON 及搜索产物不包含撤回哨兵文本；单修订历史导航保留 | `npm run verify:export` |
| 容量构建 | 1000 个问题、3000 份答案、20 层追问，验证 447 ms、构建及索引 14.865 s | `npm run benchmark -- --build` |
| 容量产物 | 72,992,051 字节，20,041 文件 | `.local/benchmark-report.json` |
| 深层浏览器验收 | 桌面、手机 2 项通过；20 层定位正确，初始目录最多 100 行 | `BENCHMARK_BROWSER=1 TEST_DIST_DIR=.local/benchmark-dist TEST_BASE_PATH=/QandA npm run test:e2e -- benchmark.spec.ts` |

耗时是本机一次测量，不代表 GitHub runner 或所有浏览器的性能。容量文本为合成测试样本，不包含真实大附件或超长聊天。手机测试是 iPhone 13 尺寸的 Chromium 仿真，未等同真机 Safari 验收。

核心测试覆盖：历史版本与不可变记录、祖先闭包和主链环、归档继承、路径复制、未知元信息、撤回投影、依赖上下文隐藏、Markdown 清洗、来源幂等收据、审核目标重检、草稿冲突、事务恢复、误导文件路径、关联方向和驳回候选过滤。

浏览器检查覆盖：标签与历史页面、深层树导航及兄弟分支隔离、复制内容、答案比较、中文搜索、GitHub 表单参数、关系图元素及来处链接、窄屏溢出和运行错误、关闭 JavaScript 后沿普通链接阅读。投稿测试拦截 GitHub 请求，没有伪造公开投稿。

当前格式化器曾使条件表达式中的 HTML/SVG 标签消失。已恢复图形关键元素，增加 DOM 验收，并禁止格式化 `.astro` 文件；这是一个已知工具限制。

## 发布前后仍需确认

- [ ] 所有者确认 GitHub 仓库及已有提交历史的公开范围；配置 Pages 为 GitHub Actions。
- [ ] 合并经过审核的首版源码，线上 Actions 成功，Pages 实际地址可访问。
- [ ] 使用一个真实访客账号完成「根问题 → 答案 → 追问 → 审核 → 站点展示」；记录实际来源，禁止伪装用户投稿。
- [ ] 在真实手机 Safari/Chrome 完成搜索、深层导航、比较和复制检查。
- [ ] 执行一次线上撤回发布，验证站点、JSON、搜索及保留路径；涉及旧版本或 Issue 的清理由维护者按实际情况处理。
- [ ] 维护者实际做一次内容回退，确认旧部署不覆盖新部署。

这些项目依赖账号、访问设置或真实人员参与，不能用本地自动化通过代替。私有预览可先用于审阅产品和示例内容。

## 两周试用记录模板

建议先邀请少量愿意探索相同主题的读者；以下均为待观察指标，不预填成功数据。

| 指标 | 第 1 周 | 第 2 周 | 观察与行动 |
| --- | --- | --- | --- |
| 实际访客 / 回访者 | 待记录 | 待记录 | 是否有持续阅读价值 |
| 提交问题 / 答案 / 追问的人数 | 待记录 | 待记录 | GitHub 跳转是否造成流失 |
| 投稿到首次处理的时间 | 待记录 | 待记录 | 审核负担是否可持续 |
| 读者能否独立找到父答案与另一分支 | 待记录 | 待记录 | 树目录是否易懂 |
| 每 20 条候选中有价值的关联数 | 待记录 | 待记录 | 按人工判断记录，不把相似度当准确率 |
| 未知来源、上下文缺失与更正次数 | 待记录 | 待记录 | 表单和元信息是否足够 |

试用结束根据实际反馈决定是否补站内投稿后端、语义检索或模型调用。当前代码为这些增强保留稳定实体与原始内容，不预先制造使用成效结论。
